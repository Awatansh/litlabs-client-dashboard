# Frontend App

The canonical project documentation lives in the repository root [`README.md`](../../../README.md).

This directory contains the Next.js application that powers the LitLabs client dashboard.

## Local frontend-only command

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` in `.env.local`. A sample file is provided at `.env.local.example`.
